#!/usr/bin/env bash
# Запуск Support Assistant в DEMO-режиме, начисто.
#
# Сносит всё (venv, БД, .env, токен, кэши), затем поднимает .env.demo
# и засеивает 200 демо-тикетов. Самый «чистый» способ показать продукт
# с нуля без следов прошлых попыток.
#
# Спросит подтверждение перед удалением (если не задан --no-prompt).

cd "$(dirname "$0")"
exec ./run.sh --demo --fresh "$@"
